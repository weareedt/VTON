# Hugging Face token (FREE) — powers the default "Accurate" engine (IDM-VTON Space).
# Optional but strongly recommended: without it, anonymous calls hit GPU quota fast.
# Get one free at huggingface.co → Settings → Access Tokens → New token (read scope).
HF_TOKEN=

# FASHN v1.6 — paid alternative for production. Only needed if you switch the
# "Accurate" engine back to FASHN. Without it, the FASHN path returns a graceful 503.
FASHN_API_KEY=

# Upstash Redis (successor to Vercel KV) — link an Upstash Redis store from the
# Vercel Marketplace and these are auto-injected. Used only to hold the result
# image briefly for the QR download (short TTL, auto-purged). Optional: without it
# the QR is skipped and client-side download still works.
# For local `vercel dev`, pull these with `vercel env pull`.
# Either naming scheme works (KV_* or UPSTASH_REDIS_REST_*).
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Nano Banana (Gemini Flash Image) — powers the "Creative" engine.
# Get a FREE key at Google AI Studio (aistudio.google.com). The default model
# gemini-2.5-flash-image has a free tier (~500 images/day).
GEMINI_API_KEY=
# Optional: override the model. Set to gemini-3.1-flash-image for the paid
# "Nano Banana 2" (better try-on quality). Leave unset for free gemini-2.5-flash-image.
GEMINI_MODEL=
# Optional: override the try-on instruction prompt without a code change.
# Leave unset to use the built-in default.
NANOBANANA_PROMPT=
